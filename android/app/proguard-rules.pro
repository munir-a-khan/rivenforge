# Release minification is off for this scaffold (see app/build.gradle.kts).
# Add keep rules here if you enable R8/ProGuard later. OkHttp and ZXing ship
# their own consumer rules, so the defaults are usually enough.
